# sqs-consumer-project
SQS consumer using goroutines for multiple queues 

# TODO improvments
Use Viper package for ENV variables, could improve handling of ENV variables on a multi cloud (dev/prod/stage)